import { ImageCard, PricingSection } from "@/features/template";
import React from "react";

type Props = {};

const Template = (props: Props) => {
  return (
    <div>
      <ImageCard />
      <PricingSection />
    </div>
  );
};

export default Template;
